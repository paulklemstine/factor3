# NETWORK/LLM RESEARCH LOOP — cron prompt (started 2026-08-12; replaces the factoring loop)

Run one iteration of the NETWORK RESEARCH LOOP (per memory file network-lab-state.md):
(1) propose a NEW testable neural-network / LLM hypothesis on ANY improvement axis —
    compression (quantization, pruning, low-rank, distillation), speed (inference,
    attention cost, sparse methods), depth (scaling, gradient flow, residual stream),
    performance (generalization, loss curves, architecture), interpretability
    ("pulling the formula out of the black box" is ONE example, not the focus), or any
    other novel, potentially world-class angle — the loop should EXPLORE ALL of these,
    not settle on one;
(2) FIRST scan the Catalog for prior work (alethean.org/package_index.js + any local
    catalog) so we build on / do not duplicate prior results;
(3) implement and run a concrete PyTorch experiment in /tmp (CPU-feasible);
(4) validate honestly against the network-loop barriers:
    (a) circularity (did we just recover what we injected?),
    (b) known-method-in-disguise (re-labeled existing technique?),
    (c) toy-scale (does it survive nontrivial sizes/domains?),
    (d) data leakage (test signal reaching training?),
    (e) variance/reproducibility (law holds across seeds/widths/data?),
    (f) measurement errors (correct metrics, statistics, nulls),
    (g) baseline unfairness (compared against fair strong baselines),
    (h) practical relevance (real win, not just toy);
(5) record: append notebook Part, bump assessment version+count, write paper to
    NetworkMathematics/ if novel, publish as GitHub issue (label approved-direction) at
    paulklemstine/Lean, commit+push;
(6) iterate. If the loop is already complete, stop and report.

# LAB PROTOCOL (same rigor as the factoring lab): exact measurable laws over a clean
# hypothesis, honest negative results recorded, all 8 barriers checked every iteration,
# commit+push each iteration with Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>.
# Status: NET-12 done (issue #107): the joint-aware-allocation real-scale check.
# On the same real causal LM (d=4 s0, 5 Gutenberg novels, dm=64, vocab 4097,
# ctx 128, full acc reproduces 0.1571 a third time), three connected results:
# (1) JOINT-MARGINAL MAP: with everything else at the role schedule, attention
# (all 16 matrices) is EXACTLY indifferent at 2 bits (retained flat 0.878),
# embed 3-bit jointly retains 0.849 (isolation ≈0.95 — compounding is real),
# readout un 2-bit collapses 0.280, MLP 2-bit 0.80–0.85/3-bit fine.
# (2) PER-TENSOR GREEDY STRICT-LOSSLESS FRONTIER = 5.31 avg bits (retained
# 0.982), interface embed/pos/un PINNED AT 6 (73% of params); even all-4
# misses the 0.98 bar per-tensor (0.979). (3) PER-CHANNEL (per-row) RTN is the
# fix: uniform all-4 is LOSSLESS at 4.00 bits (per-row retained 0.987, 1.3 bits
# below the per-tensor frontier); per-row uniform-3 still fails (0.947) — the
# 4-bit interface is irreducible under BOTH primitives; uniform-2 hopeless
# (0.588); the per-tensor-optimized frontier scores WORSE per-row (0.973) —
# allocation is primitive-dependent. ROTATION QUESTION answered: any data-free
# schedule reach lossless? YES — per-channel uniform-4 (4.0 bits). Is 4-bit
# interface + 2-bit interior enough? NO — "4iface/all-interior-2" 0.733 @ 3.46,
# "4iface+MLP4+attn2" 0.907 @ 3.82 (the 8pt gap ≈ attention 2→4 ⇒ "attention
# 2-bit free" is an OPERATING-POINT artifact: invisible at the degraded 0.878
# baseline, ~8pt cost in a clean joint net). NET-11's "no static schedule ≤3.7
# bits lossless" CORRECTED to: per-tensor ~5.3 bits, per-channel uniform-4.0.
# 12 experiments, assessment v12. Load-bearing boundary still needs
# NON-POSITIONAL stack content / s≫64 / unbounded nesting WITH CAUSAL MASKING.
# NEXT (compression): activation-aware (outlier) allocation / per-channel 3-bit
# with outlier retention / interface floor at a larger real LM (d=8, bigger dm)
# — does the 4-bit interface floor hold, or do activation-aware methods break
# it? OR carry chain at scale OR attention-cost laws (speed axis). Compression
# has had 2 real-scale iterations, speed 1, depth 8.
