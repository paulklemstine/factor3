# NETWORK/LLM RESEARCH LOOP — cron prompt (started 2026-08-12; replaces the factoring loop)

Run one iteration of the NETWORK RESEARCH LOOP (per memory file network-lab-state.md):
(1) propose a NEW testable neural-network / LLM hypothesis on ANY improvement axis —
    compression (quantization, pruning, low-rank, distillation), speed (inference,
    attention cost, sparse methods), depth (scaling, gradient flow, residual stream),
    performance (generalization, loss curves, architecture), interpretability
    ("pulling the formula out of the black box" is ONE example, not the focus), or any
    other novel, potentially world-class angle — the loop should EXPLORE ALL of these,
    not settle on one;
(2) FIRST scan the Catalog for prior work (alethean.org/package_index.js + any local
    catalog) so we build on / do not duplicate prior results;
(3) implement and run a concrete PyTorch experiment in /tmp (CPU-feasible);
(4) validate honestly against the network-loop barriers:
    (a) circularity (did we just recover what we injected?),
    (b) known-method-in-disguise (re-labeled existing technique?),
    (c) toy-scale (does it survive nontrivial sizes/domains?),
    (d) data leakage (test signal reaching training?),
    (e) variance/reproducibility (law holds across seeds/widths/data?),
    (f) measurement errors (correct metrics, statistics, nulls),
    (g) baseline unfairness (compared against fair strong baselines),
    (h) practical relevance (real win, not just toy);
(5) record: append notebook Part, bump assessment version+count, write paper to
    NetworkMathematics/ if novel, publish as GitHub issue (label approved-direction) at
    paulklemstine/Lean, commit+push;
(6) iterate. If the loop is already complete, stop and report.

# LAB PROTOCOL (same rigor as the factoring lab): exact measurable laws over a clean
# hypothesis, honest negative results recorded, all 8 barriers checked every iteration,
# commit+push each iteration with Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>.
# Status: NET-14 done (issue #109): the compression axis is EXHAUSTED at small
# real-LM scale. The last lever — activation-aware quantization WITH calibration
# passes (AWQ/SmoothQuant per-channel ACTIVATION scales from 512 train-seq
# calibration) — is a NO-OP-TO-NEGATIVE on the same real causal LM (d=4 s0, full
# acc reproduces 0.1571 a FIFTH time): (1) the per-channel activation scales are
# near-FLAT (max/mean ≈ 1.2: un 2.13/wq0 2.12/mi0 2.12/mo0 1.82) — AWQ's
# absorption needs 10–100× channel heterogeneity, which this scale lacks
# (activation-side mirror of NET-13's flat weight outliers); (2) AWQ uniform-3
# best α=0.5 = 0.943 vs plain per-row 0.947 (no help), α=1.0 = 0.888 (much
# worse), uniform-4 identically lossless 0.987; (3) interface-at-3 with AWQ
# scales still 2.2pts short (0.958 @ 3.18 bits); (4) activation-informed
# allocation much WORSE than weight-based (0.828–0.841 vs role 0.892 / uniform-4
# 0.987). ⇒ the 4-bit interface floor is BOTH data-free-irreducible (NET-13:
# axis/split/clip all fail, no outlier mass top-1% ~3.5%) AND
# activation-irreducible (NET-14); per-channel uniform-4 (4.00 bits, data-free,
# retained 0.987) is the practical optimum — every primitive (per-tensor 5.31,
# per-row/per-column uniform-4, magnitude-split, percentile-clip,
# activation-calibration) leaves the interface irreducible below 4 bits.
# Previous: NET-13 (issue #108) data-free levers refuted; NET-12 (issue #107)
# per-tensor frontier 5.31 / per-channel uniform-4 lossless.
# 14 experiments, assessment v14. Load-bearing boundary still needs
# NON-POSITIONAL stack content / s≫64 / unbounded nesting WITH CAUSAL MASKING.
# NEXT (rotate away from compression — 3 real-scale iterations exhausted it):
# SPEED axis — carry chain at scale / attention-cost laws (the speed axis has
# had only 1 real-scale iteration, NET-10); OR the one open compression
# question: does the 4-bit interface floor shift at LARGER scale (d=8, bigger
# dm, real causal LM); OR load-bearing depth (non-positional stack content with
# causal masking). Compression 3 real-scale iterations, speed 1, depth 8.
