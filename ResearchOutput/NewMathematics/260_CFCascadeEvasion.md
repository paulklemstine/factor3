# Paper 260 — CF-CASCADE EVASION SHOT: **PRIMARY-NULL SUPPORTED (gated), AND THE v₂(p+q) "SIGNAL" IS FULLY ABSORBED BY N mod 8** — No Non-SQUFOF CF-Prefix Feature Carries Detectable Information About the Fermat Oracle Before the SQUFOF Zone (Zero Replicated Events, Both Populations × 8 Depths × 8 Features); the ν₂(Q_j)-Profile Association With v₂(p+q) Is Real at ~0.99 Bits Plain but Drops to EXACTLY 0.0 Conditioned on N mod 8 — Paper 198's Mod-8 Cap Stands; the Geodesic-Sensor Face Seals at Toy Strength

**Verdict name: INVALID_CONTROLS(PC2) WITH PRIMARY-NULL SUPPORT + FULLY-
ABSORBED SECONDARY** — recorded in three honest layers.

Round-96 #2 · fleet Bet #3. Sources: `ResearchOutput/scripts/
2026-08-25-round75/{exp609_cf_cascade.py, exp609_result.json, exp609_run.log,
exp609_findings.md}`. Populations: bits {48, 40} × seeds {20261113, 20261114}
(registry-verified unused), n = 800 factored balanced semiprimes each, bands
SEED+{283e6, 293e6} asserted above the true prior ceiling. Pre-registration
pinned (`69d8e6a`) before data; PC2 amended pre-evidence (uncensored pass);
all mechanical defects caught by crashes/gates before recording.

## 1. Primary face (geodesic sensor): null supported

Across both populations, all 8 registered depths K ∈ [16, med_first_square/2],
and 8 non-SQUFOF cascade features: **zero replicated q99-significant events**
(one marginal 0.011-bit blip, non-replicated → dead by rule). The √N
continued-fraction prefix layer carries no detectable information about the
Fermat oracle variable d ≤ B before the SQUFOF zone. Formal status gated by
PC2 (below), but the failure mode of a too-tight null is excluded by §3's
detectability demonstration.

## 2. Secondary face: real association, trivial source

The ν₂(Q_j)-profile feature max_nu2Q carries **≈ 0.99 bits** about v₂(p+q)
classes {1, 2, ≥3} plain (MI 0.998 vs null q99 0.017; replicated exactly in
both populations) — on its face the first new adic content since round-71.
Conditioning the permutation nulls on **N mod 8 drops it to EXACTLY 0.0 bits
in both populations**: the entire association is absorbed by trivial modular
arithmetic. Paper 198's mod-8 cap stands; the cascade adds nothing beyond what
one residue read already gives. (mod-64-conditioned figures ≈0.86b are
DEGENERATE — 400 strata starve an n = 800 permutation null — disclosed,
not interpreted.)

## 3. PC2 erratum (why the gate reads INVALID_CONTROLS)

The registered estimator-validation control demanded that the SQUFOF
first-square-index feature carry bits about y* = 1{d ≤ B} — a WRONG-TARGET
association: square forms factor N through SQUFOF's own machinery, not through
small Fermat distance. Measured association ≈ 0.02 bits, below q99, both
populations. The estimator-validation PURPOSE is served instead by §2's
massive detectable signal against tightly-calibrated nulls. PC2′ pre-stated
for future evidentiary use: control associations must have a stated MECHANISM
(e.g., first-square-index predicting actual SQUFOF success).

## 4. Ledger catches

PC2 censoring by its own depth cap (v2 amendment added the uncensored pass);
wrong-target PC2; mi_bits signature misuse; ctrl tuple/float mismatch;
argmax grid-position indexing. All caught by gates/crashes pre-evidence;
pinned-seed populations make every rerun bit-reproducible. Head-to-head arm
DEFERRED/OPEN (registered as such).

## 5. Barrier validation and consequence

No barrier breached; negative strength again. The geodesic-sensor face seals
at toy strength pending only the formal gate repair (PC2′), and paper 198's
adic cap is confirmed against its first serious challenger. Consistent with
the conic trap's lesson generalized: ANY readout whose information collapses
onto coarse modular invariants was never new content. Falsifiable follow-ups:
(a) PC2′-gated rerun at bits {56, 64} with deeper grids; (b) head-to-head
matched-budget arm vs direct E(a) sampling; (c) class-group linkage test —
the one unreached mechanism named by the fleet brief.
