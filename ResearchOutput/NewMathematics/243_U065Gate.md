# Paper 243 — U065-FRESH-SEED-GATE [FINAL]: **GATE-H0 FIRES — THE NON-DIVISIBILITY POSITIONAL FEATURE DOES NOT REPLICATE** on Fresh Seed 20260902 (amp_mix 0.074 ± 0.038, Registered Raw z = 1.97; Null-Calibrated z_cal = **−1.08** vs Original +1.53; Raw 1.97 vs Original 4.11) — Pipeline FULLY EXONERATED (Generator EXACT_MATCH vs Stored Lineage, ALL_DISJOINT vs All Six Prior Seeds, CTRL-A Clean) — **PAPER 242's MAP ENTRY AMENDED TO SINGLE-SEED-UNCONFIRMED**, Its z = 4.11 Flagged Possibly Max-Statistic-Sweep-Inflated — What Replicates: the κ Composition Layer (Top and Bottom Cells Coincide, Spread 0.649–1.432 vs 0.645–1.406)

**Verdict name: GATE-H0 — FEATURE DOES NOT REPLICATE** (registered tree evaluated in order
H1 → H0 → PARTIAL; fired exactly as pre-registered, with both significance scales reported per
the paper-242 caveat).

Round-87 #2 · exp 592 · applies tonight's fresh-seed-gating lab rule **to tonight's own newest
output**: paper 242 (exp 588c) was recorded hours earlier the same day carrying a NEW MAP ENTRY
(non-divisibility positional mechanism) resting on ONE seed lineage, and the rule demands
fresh-seed gating before any such claim hardens. This experiment reran the IDENTICAL pipeline
(statistics verbatim exp588c, generation verbatim exp578 code path) on an independent lineage and
applied the two pre-named gates. **The entry did not survive its own gate.**

Sources: `ResearchOutput/scripts/2026-08-24-round74/exp592_{u065_freshseed.py, smoke.log,
smoke_result.json, full_run.log, result.json, positions.npz}` (positions npz sha256
`a3aae281…d146`; wall **350.8 s**; cluster bootstrap 2000 reps, seed 20260901, exp582
convention). **No `exp592_findings.md` exists** although the brief listed it — findings were
returned inline per the standing subagent policy; `exp592_result.json` (config / regression /
residual rows / stats / verdicts / honest_notes / wall_s) is the authoritative record.

## 1. Pre-registration verbatim (written before any analysis)

From the `exp592_u065_freshseed.py` header:

> GATES (fixed NOW, before any fresh-seed number exists; evaluated in order H1 -> H0 -> PARTIAL):
>   GATE-H1 (feature replicates): fresh-seed amp_mix >= 1.0 * se_mix  AND  null-calibrated
>     z_cal >= 2  =>  non-divisibility mechanism CONFIRMED TWO-SEED; paper-242 entry HARDENS.
>   GATE-H0 (fails to replicate): bootstrap 95% CI of amp_mix includes 0  OR  z_cal < 1
>     =>  entry stays SINGLE-SEED-UNCONFIRMED; the original z=4.11 is FLAGGED as possibly
>     inflated by max-statistic selection across the sweep.
>   Neither gate => PARTIAL band: entry stays single-seed-unconfirmed (no inflation flag).
>   Units fixed NOW: "amp >= 1.0" reads as multiples of the registered cluster-bootstrap se_mix
>   (i.e. registered raw z_mix = amp_mix/se_mix >= 1); CI = 2.5/97.5 percentiles of the 2000-rep
>   N-resample bootstrap distribution of amp_mix; z_mix = amp_mix/se_mix is the REGISTERED RAW
>   scale, z_cal = (amp_mix-amp_sim)/sqrt(se_mix^2+se_sim^2) vs CTRL-B is the NULL-CALIBRATED
>   scale (paper-242 caveat honored: BOTH scales reported going forward; disagreement flagged,
>   never resolved).

Method fixed in the same header: FRESH MASTER SEED 20260902 (distinct from the six prior
lineages 20260824/25/26/27/28/31); population = 128 balanced bitlen-96 semiprimes via exp578
code path verbatim (`random.Random(master)`, getrandbits(48)|top|1 → next_prime, q≠p retry);
window jlo=isqrt(N)+1 … jhi=3·isqrt(N); per-chunk rng(master+7000+c), 8 chunks × 16 N, sequential
150k-draw replay, ONE rng per chunk; exp569 gcd-chain tester (cut 1e6, P5→P6) classifies hits;
paired non-hits capped 4000/N first-in-stream; model/statistics VERBATIM exp588c (16-cell
divisibility mixture, flank-only κ with λ=5 shrinkage, score window t ∈ [0.55, 0.75], flanks
t<0.40 | t>0.85, nb=50, 3-bin smoothing, buffer bins predicted-but-not-scored). Amendment log:
**none** — registration completed before any fresh-seed number existed.

## 2. Pipeline exoneration (all checks gated BEFORE statistics; none failed)

The negative verdict is only meaningful if the fresh run could have detected the feature. Every
escape route was closed first, gated on `abort_before_statistics` (armed, never fired):

| Check | Result | Why it matters |
|---|---|---|
| Generator regression vs STORED exp581/exp588c lineage (master 20260828) | **EXACT_MATCH** — population windows int64-exact for all 128 Ns, chunk-0 stream membership AND order walk exact (proven exp588b-A3 checks) | the code path is byte-faithful to the lineage where z = 4.11 was measured |
| Freshness / disjointness | **ALL_DISJOINT** vs ALL SIX prior lineages (each regenerated in full; pairwise N-set disjointness asserted; sha256[:16]-of-sorted-N-repr recorded for every lineage — fresh `0d69e6f4e59ebc9e` vs priors `ac7af607…`, `33362a01…`, `cbcf3057…`, `d3185d37…`, `20115284…`, `bc72fe38…`) | the new data shares zero population mass with any prior measurement |
| CTRL-A machinery null (count-vs-count halves, identical machinery incl. max-stat + bootstrap) | amp 0.0221 ± 0.0092, max-dev over ALL bins 0.0336 — **PASS** | estimator machinery clean at full sample size |
| Volume match | 9840 hits / 512,000 reference over 128 Ns (mean 76.88 hits/N; global flank rate g = 34.44/Dickman-unit) — comparable to stored lineage's 9594/512,000 | power comparable to the original detection |

Disclosed scope note (honest_notes): an in-process fresh lineage has NO stored npz to regress
against, so the regression targets the GENERATOR (validated against the stored lineage's npz) —
which is exactly the right direction for a freshness gate: code path proven identical, data proven
new. Two checklist deviations disclosed below (§6): smoothness spot-validation dropped as vacuous;
hash-convention change.

## 3. Fresh-seed result — both scales

| Quantity | Fresh seed 20260902 (this gate) | Stored lineage 20260828 (paper 242) |
|---|---|---|
| amp_mix (score window t∈[0.55,0.75]) | **0.0742 ± 0.0377** | 0.1774 ± 0.0432 |
| Registered raw z_mix | **1.97** | 4.11 |
| Bootstrap 95% CI of amp_mix | [0.0451, 0.1959] (excludes 0) | — |
| CTRL-B estimator-null amp_sim | 0.1398 ± 0.0478 | 0.0860 ± 0.0411 |
| Calibrated excess (amp_mix − amp_sim) | **−0.066 ± 0.061** | +0.091 |
| **Null-calibrated z_cal** | **−1.08** | +1.53 |
| Single-α baseline amp_orig (same pipeline) | **0.0148** (≈ vanished) | 0.1163 |
| Wide-window amp_mix_wide [0.45, 0.85] | 0.1980 (non-bearing) | 0.1774 |

Gate evaluation, exactly as registered: GATE-H1 requires amp ≥ 1·se AND z_cal ≥ 2 — fails (z_cal
= −1.08). GATE-H0 fires on its second clause (z_cal < 1; the CI-includes-0 clause is NOT met —
flagged, never silently resolved). **Verdict: GATE-H0 — FEATURE DOES NOT REPLICATE.**

The decisive internal reading: the fresh RAW amplitude (0.074) is SMALLER THAN THE FRESH MEASURED
NULL BIAS (CTRL-B amp_sim = 0.140) — a pure null draw through this estimator produces amplitudes
of the observed size, and the calibrated excess is negative. Stronger still, even the UNMIXED peak
collapses on the fresh seed (amp_orig 0.0148 vs 0.1163): the mid-window hump itself, not just its
post-mixture residual, fails to appear. The registered raw-scale CI excluding zero is precisely
the max-over-bins positive bias the paper-242 caveat warned about — on the calibrated scale there
is nothing to explain.

## 4. AMENDMENT to paper 242 (explicit)

1. **Entry status change:** paper 242's NEW MAP ENTRY — *non-divisibility positional mechanism*
   (third orthogonal layer beside positional 228–230 and left-edge composition 238–240) — is
   hereby **AMENDED TO SINGLE-SEED-UNCONFIRMED**. It no longer carries entry status on the
   scale-smoothness map.
2. **Inflation flag:** paper 242's verdict-bearing raw z = 4.11 is **FLAGGED AS POSSIBLY
   MAX-STATISTIC-SWEEP INFLATED**. Grounds: the discovery chain (paper 232's sweep across ~30
   fit cells, carried through 241→242 unchanged) was itself a selection surface; CTRL-B measures
   a null amplitude of the observed magnitude on BOTH lineages; and the two independent seeds
   bracket the value in exactly the direction selection-plus-max-bias predicts (original above
   its null, fresh below its larger null; calibrated scale +1.53 → −1.08 straddling zero).
   Per the registration this is a FLAG, not a proof of artifact — the original measurement stands
   as recorded history of its lineage.
3. **What survives untouched — the composition layer is CONFIRMED TWO-SEED:** the κ cell
   structure replicates on the fresh seed. Top cell IDENTICAL (**2|v ∧ 3|v ∧ 5|v ∧ 7∤v**, κ/g =
   1.4315 vs 1.406), bottom cell IDENTICAL (all-cleared, 0.6491 vs 0.645), spread matched within
   ~2% (0.649–1.432 vs 0.645–1.406, ~2.2× modulation), and composition again FLAT in t (max cell
   drift 0.204% vs 0.269%). **The "divisibility is a RATE DIAL, not a position dial"
   formulation STANDS on two seeds**; only its corollary — that a non-divisibility POSITIONAL
   residual remains after the mixture — is downgraded to unconfirmed.
4. **Chain history intact:** papers 232 → 241 → 242 remain accurate as records of measurements on
   their lineages; nothing else in the barrier map is touched by this amendment.

## 5. Named reopens-condition (the only door back in)

If anyone reopens the entry: a **pre-registered single-hypothesis test at the FIXED vertex
u* = 0.65** — no sweep, no post-hoc vertex selection, hypothesis and bar fixed before data — on
**≥ 3 pooled independent fresh seed lineages**, powered against the CTRL-B-calibrated scale (per
paper 242's §5 note). Only a pass under that protocol hardens the entry; sweeps and post-hoc
vertices are how the flag in §4.2 arose.

## 6. Ledger catches (all disclosed)

1. **Smoke-grid CTRL-A trips on small-sample noise alone** (smoke 24 Ns × 20k samples/N: CTRL-A
   amp 0.0362 ± 0.0544, maxdev 0.1238 > 0.10 bar → smoke correctly read ARTIFACT-CONTAMINATED /
   inconclusive per the registered evaluation order; full-run CTRL-A maxdev 0.0336 PASSES).
   Bar unchanged; smoke is plumbing/calibration only (exp578 precedent) and no verdict was drawn
   from it. The catch is the bar's small-sample sensitivity, now on record.
2. **Smoothness spot-validation dropped as VACUOUS in-process:** fresh hits are DEFINED by the
   same exp569 tester that would "validate" them — the check is circular on fresh data and was
   discarded (disclosed deviation from the exp588c checklist; no information lost).
3. **Hash-convention change:** freshness hashes use sha256[:16]-of-sorted-N-repr (own
   convention) because exp578's triple-based pop_hash needs lo/hi, which build_pop_v discards;
   direct pairwise N-set disjointness against all six regenerated lineages asserted alongside —
   the disjointness claim rests on set disjointness, not hash equality.
4. **Generator-vs-stored regression direction** (§2 scope note): validates the CODE PATH, not the
   fresh data — correct and sufficient for a freshness gate, stated to prevent overreading.
5. **Scale disagreement handled per registration:** raw CI excludes zero while calibrated z_cal <
   0 — flagged, never resolved; the verdict-bearing clause is the REGISTERED H0 trigger (z_cal<1),
   and both scales travel in every downstream citation of this result.
6. Standing disclosures: reference stream is a capped subsample (≤4000/N first-in-stream),
   unbiased for uniform-sampling composition; bit 0 (2|v) identically j-parity since N odd;
   ln v computed from EXACT integer v then float-converted; buffer bins predicted-not-scored;
   boot seed 20260901; only exp592_* files written during runs; no commits during runs.

## 7. Barrier validation

No breakthrough claimed — the opposite: a NEGATIVE replication gate that REMOVES an entry from
the map rather than adding one, demonstrating tonight's honest-map rule applied to tonight's own
newest output within hours (record-first culture includes un-recording). Untouched: residue cap
4/3 theorem; scan-order position 5.19×; external class-hint law 1/(1−(1−θ)P_hit); external
interval-hint coverage × width law; quantum frontier closed; method stratum map; abelian pinning
ladder; QS calibration; utility closure; four-class rate-residual closure (paper 237);
papers 238–240 spike-origin resolution. Asymptotic relevance per the standing directive: killing
a non-replicating positional-mechanism claim early prevents downstream power spend on a phantom,
and the SURVIVING layer — divisibility rate dials, now two-seed — is scale-carrying
(composition transfers across bit lengths unchanged). Open frontiers unchanged: non-QR per-N
structure at u = 2.5 (31%-above-floor residual), factor-local methods outside scan-order framing,
MA-1 effectivity; paper 238's .2346 provenance flag still travels. The named
non-divisibility-carrier follow-up (paper 242 §7) is CLOSED as unconfirmed unless the §5
protocol fires.

## Attribution

Experiment + analysis artifacts: `ResearchOutput/scripts/2026-08-24-round74/`
(exp592_u065_freshseed.py — pre-registration + empty amendment log in header, authored before
first execution; exp592_smoke.log/_result.json/_positions.npz; exp592_full_run.log;
exp592_result.json — config/regression/residual rows/stats/verdicts/honest_notes/wall_s
[authoritative]; exp592_positions.npz sha256 a3aae281…d146; no findings.md — inline findings per
subagent policy, disclosed). Recorded round-87 #2; notebook Part 285; assessment v350; issue #391.
