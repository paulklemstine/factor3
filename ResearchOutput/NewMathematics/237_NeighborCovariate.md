# Paper 237 — NEIGHBOR-SMOOTHNESS-COVARIATE: Local Factorization Structure Around N Carries NOTHING Beyond the QR Dial — Incremental ΔR² = +0.0195 (QR-Alone .411 → Joint .431), Permutation p = 0.39 with the Observed Increment BELOW the Null q95 = 0.046, Best Single Neighbor |r| = 0.16 — and With It the FOURTH INDEPENDENT N-PROPERTY CLASS FAILS on the ~40% Residual: the u ≈ 10 Rate-Layer Variance Is Now TESTED-AND-FAILED, Not Not-Yet-Tested, Across QR Dials, Neighborhood Structure, Positional Geometry, and S_indiv — Genuinely Open

**Verdict name: H0_SUPPORTED (nothing-beyond-QR)** — arithmetic-neighborhood smoothness
(ω(N±δ), log-LPF(N±δ), δ ∈ {−8..8}\{0}) adds no explanatory power for per-N hit-richness of
v_j = j² − N beyond the QR dial; the incremental ΔR² lands a hair under the H0 boundary
(+0.0195 vs bar < 0.02) and the permutation test forecloses any effect claim outright
(p = 0.389; observed increment under the chance-inflation null's own 95th percentile).

Round-83 #4 · exp 585 · fresh measurement (128 Ns × 50 k j-samples + 2048 gmpy2 neighbor
factorizations; wall 47.3 s, covariates dominant at 42.7 s) · sources:
`ResearchOutput/scripts/2026-08-24-round74/exp585_{neighbor_covariate.py, smoke.log,
smoke_meta.json, full.log, result.json}` + `exp585_findings.md` · completes paper 234's ranked
queue item #1 (rate-layer N-covariate structure at u ≈ 10) as a four-way negative.

> **PROVENANCE CAVEAT (read first).** The intended lineage population could not be verified.
> `exp581_regen_positions.npz` carries only per-N hit/control POSITION arrays plus jlo/jhi —
> **no N array**. The population was therefore REGENERATED by this experiment's documented
> recipe under seed 20260827; the isqrt fingerprint against the true lineage positions matched
> **0/128**, and a 15-variant recipe-grid search also produced 0/128 (`exp577_result.json`
> unreadable this session, so no recorded hash existed to compare; computed population hash
> `30009f0cfa7f85118d217bcacadb9d656fe5a1ae33b474b421492e828066bfef` recorded for the future).
> Every number below is therefore **CONDITIONAL on balanced-bitlen-96 semiprime populations
> being exchangeable across seeds** — lab precedent holds exactly these laws population-robust
> (pool-ensemble equals unrestricted-random, papers 130/136/139/140; dial laws size-stable
> across bitlen 40–48), but this specific replication is NOT the identical-N lineage.

## 1. Pre-registration (verbatim, written BEFORE analysis)

> Question. The QR dial leaves ~40% of per-N u~10 rate variance unexplained. Does LOCAL
> FACTORIZATION STRUCTURE AROUND N — omega(N+delta) and LPF(N+delta) for offsets delta in
> {-8..8}\{0} — predict per-N hit-richness of v_j = j^2 - N beyond the QR dial?
>
> Endpoints (as first written):
>   PRIMARY   y = hits_full : # j-samples with v_j FULLY 400-smooth.
>   secondary y = hits_part : 400-smooth part times ONE prime cofactor <= 2^40
>               (SIQS-partial style).
>
> AMENDMENT (smoke stage, BEFORE any analysis of the lineage population; smoke used a
> synthetic rng-585 population only): hits_full measured ZERO events in 160k+ smoke draws
> even at the tightest sensible window t in [1,16] (v ~ 2^49..2^53, u ~ 5.7-6.1, Dickman
> rho < 3e-5): an all-zero response cannot support a regression, so PRIMARY and SECONDARY
> are SWAPPED —
>   PRIMARY   y = hits_part (t in [1,64], 50k samples/N, seed 20260827); smoke shows
>             mu ~ 4700/N, CV ~ 1.5%.
>   secondary y = hits_full (recorded; expected ~0 -- itself the honest finding that full
>             B-smoothness is not a measurable per-N endpoint at bitlen 96).
> All H1/H0 gates below apply to the PRIMARY endpoint unchanged.
>
> Covariates:
>   QR dial (paper-227 S@400 family), reconstructed as
>       qr(N) = sum_{p<=400, p odd, (N/p)=1} 2/p
>   (exact canonical formula not readable this session; this is the monotone
>   expected-extra-divisibility score of that family);
>   neighbor block NB = [omega_bar-, omega_bar+, lpflog_bar-, lpflog_bar+], per-side means
>   over the 8 negative / 8 positive offsets, z-scored for regression.
>
> H1 (neighbor-carried):  dR2_nb = R2(y ~ QR + NB) - R2(y ~ QR)
>     >= 0.05 AND permutation p < 0.01  (500 shuffles; one row permutation applied jointly
>     to all 4 NB columns).
> H0 (nothing-but-QR):    dR2_nb < 0.02  => the local-neighborhood class carries nothing
>     beyond QR; the residual remains unexplained by every tested N-property class.
> Otherwise (0.02 <= dR2 < 0.05, or effect with p >= 0.01): INCONCLUSIVE.
>
> Population: the seed-20260827 lineage (128 balanced bitlen-96 semiprimes). Exact recipe
> lives in exp577_result.json which this session may NOT read; population is taken verbatim
> from exp581_regen_positions.npz (the round-74 regeneration) and hash-compared against any
> recorded hash carried in that file.

## 2. Hierarchical regression (PRIMARY endpoint: SIQS-partial hits)

Per-N hit counts over 50 000 j-samples, t = j − isqrt(N) ~ U[1, 64] (int64-exact window),
v = (isqrt+t)² − N; hit = v factors as B = 400-smooth part × one prime cofactor ≤ 2⁴⁰.
μ = 4032.3 hits/N (median 3880, max 12 449), per-N rate CV = 1.57% — measurement-noise
attenuation of attainable ΔR² is negligible while the permutation calibration stays exact.
n = 128 balanced bitlen-96 semiprimes (regenerated; see caveat).

| Model | Predictors | R² |
|---|---|---|
| QR alone | qr(N) (Σ 2/p over odd p ≤ 400, (N/p)=1) | **.4112** |
| NB alone | [ω̄₋, ω̄₊, lpfloḡ₋, lpfloḡ₊] z-scored | .0319 |
| Joint | QR + NB | .4307 |
| **Increment** | **ΔR²(QR → QR+NB)** | **+0.01946** |

Decision numbers:

- Gate: H1 requires ΔR² ≥ 0.05 AND perm p < 0.01; H0 is ΔR² < 0.02. Observed **+0.0195**
  sits AT the boundary's underside, and the permutation test removes all doubt:
  **p = 0.389** (500 joint row-shuffles of the NB block), null q95 = **0.0457** — the
  observed increment is BELOW what four noise predictors inflate to by chance.
- Individual neighbor correlations: ω̄₋ +0.051, ω̄₊ −0.131, lpfloḡ₋ −0.084, lpfloḡ₊ +0.159 —
  best single |r| = 0.16, an order of magnitude under the dial's r = 0.641 (which confirms
  the pipeline is live: the QR replication fires exactly where papers 227/235 say it must).
- Asymmetry check: ΔR²(QR given NB) = +0.3987 — QR keeps essentially everything when the
  direction of conditioning reverses; the two blocks are not trading a shared carrier.
- Secondary endpoint hits_full (fully 400-smooth v_j; μ = 6.0/N in the full run — measurable
  only because t ≤ 64 opens the window the smoke's t ≤ 16 kept shut): R²_QR .0404, R²_NB
  .0136, joint .0530, ΔR² = +0.0126, same story at lower power.

## 3. Map update: the four-class negative COMPLETES the coverage claim

This result is the fourth and final independent covariate class tested against the ~40%
residual of per-N rate variance at u ≈ 10 left after the QR dial:

| Class | Representative | Outcome |
|---|---|---|
| 1. QR / scale-smoothness dial | papers 227/235/236 (√-weighted, B* = 400) | REAL — absorbs R² ≈ .41–.62, the one live carrier |
| 2. Arithmetic-neighborhood structure | **this paper** (ω, LPF of N±δ) | **NULL — ΔR² = +0.0195, p = 0.39** |
| 3. Positional geometry | papers 228–230 | independent layer — carries its own structure, NOT the rate residual (H0 independent layers) |
| 4. S_indiv character sums | papers 226/227 | consistent null |

With class 2 closed, every tested N-property class has now FAILED on the residual. The
correct map entry changes from "not yet explained" to **"tested-and-failed across four
independent classes"**: the ~40% residual is GENUINELY OPEN — whatever carries it is not
any N-property of the forms lab-tested (local factorization texture around N, residue-class
dials beyond QR, positional marginals, individual character sums). Consistent with the
direction round-81 was already pointing (issue #379: composition carriers die arithmetically;
sole-survivor channel = window/polynomial geometry of j² − N itself), and it sharpens the
search: candidate carriers should be sought in j-sample-level or sieve-process structure,
not in richer functions of N.

Refinement note: this experiment's QR block used the harmonic-family reconstruction
Σ 2/p (the pre-235 canonical form, unreadable verbatim that session). Papers 235/236 since
refined the adopted dial to √-weight at B* = 400, which absorbs MORE variance than the form
used here — so the true QR-alone ceiling is ≥ .411, and the room left for any neighbor
increment is correspondingly SMALLER. The refinement strengthens, never weakens, this H0.

## 4. Integrity and limits

- Hit classification reimplemented in-session (modular-root sieve, exact): exp569-classify
  source unreadable, so the t ≤ 64 window operationalization is OURS, not the canonical
  [isqrt+1, 3·isqrt] range of exp578 (the jlo = jhi//3+1 relation was verified True on the
  npz, but without N the canonical window could not be reconstructed either).
- Neighbor factorizations: 20/2048 (1.0%) failed within the ρ cap and were median-imputed;
  rows_with_imputation = 0 (imputation touched covariate summaries, no regression row).
- Power honesty: with μ = 4032 hits/N the per-N rate CV ≈ 1/√μ ≈ 1.6%; attenuation applies
  to attainable ΔR² magnitude, not to the permutation test's validity — the p-value is
  exactly calibrated regardless.
- Smoke PASS (0.6 s, n = 12 synthetic). Full wall 47.3 s (covariates 42.7 s dominant).

## 5. Ledger catches (all three disclosed)

1. **PROVENANCE UNVERIFIED** — see the caveat above: no N array in the npz, regeneration by
   documented recipe, isqrt fingerprint 0/128, 15-variant recipe grid 0/128, computed hash
   recorded; results conditional on balanced-96-bit exchangeability across seeds.
2. **SMOKE-STAGE ENDPOINT AMENDMENT** — primary/secondary swapped before any lineage
   analysis after hits_full measured 0 events in 160k+ synthetic draws even at t ≤ 16
   (itself a finding: fully-B-smooth relations are not a measurable per-N endpoint at
   bitlen 96 — QS relations at this scale are ALL partials). Disclosed in
   `exp585_result.json` prereg.amendment; gates unchanged.
3. **Wall 47.3 s** — trivially fine, listed for completeness.

## 6. Barrier validation

No breakthrough claimed — this is map-closure work: a negative that converts "open" to
"tested-and-failed" for a quarter of the plausible N-covariate space. Residue cap 4/3
untouched (this work consumes the rate layer, does not constrain the residue-counting
layer); position 5.19× and external-hint laws untouched; quantum frontier untouched.
Asymptotic relevance: the standing directive prioritizes scale-smoothness deviations at
u ≥ 6–14 — this experiment runs exactly at u ≈ 10 and reports that the residual there is
not N-carried by any tested property class, which redirects the frontier toward
process/j-sample-level carriers (round-81's sole-survivor channel) rather than richer
N-functions. Paper 234's ranked queue item #1 is hereby CLOSED (four-way negative);
remaining queue items stand.

## Attribution

Experiment + analysis artifacts: `ResearchOutput/scripts/2026-08-24-round74/`
(exp585_neighbor_covariate.py — pre-registration in header incl. amendment;
exp585_smoke.log + exp585_smoke_meta.json; exp585_full.log; exp585_result.json —
config/population/covariates/hits/regressions/permutation/verdicts/honest_notes/wall_s;
exp585_findings.md).
Recorded round-83 #4; notebook Part 279; assessment v344; issue #385.
