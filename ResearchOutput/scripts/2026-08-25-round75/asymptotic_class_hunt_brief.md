# ASYMPTOTIC-CLASS HUNT BRIEF (2026-08-25, round 1)
Provenance: 34-agent workflow (10 lenses -> 19 proposals -> 19 hostile attacks
-> 2 revision cycles -> 2 deep-dives -> honesty-gated synthesis).
VERDICT: **19 evaluated / 17 killed / 2 downgraded / 0 passed. NO proposal
reached genuine new-asymptotic-class status.**

## Survivors (both DOWNCLASS)
1. GACS-R2 (grammar circuit-synthesis factoring): T = poly(n)*gamma_eff^{L_fac},
   space n*gamma^ceil(L_fac/2). Internally correct, externally dominated:
   gamma^Theta(n) on generic inputs (worse than GNFS); polynomial ONLY on a
   zero-density special-form sliver where family-known gcd scans are already
   poly AND faster (automation premium only). Lives/dies on bit-honest SHL
   immediate charging. Durable output: SPLIT-CONGRUENCE VACUITY LEMMA.
   Class odds ~0.03. Toy experiment spec P1-P6 pre-stated (KC4 expected).
2. Infrastructure Carry Census: Theta-tilde(N^(1/4)) -- EXACT tie with
   SQUFOF/Buchmann-Jacobson-Williams BSGS regulator tier ("BSGS wearing a
   lattice-decoded regulator as a hat"); competitive only below N~10^41.5,
   SQUFOF's historical niche. Durable output: regulator-readout micro-lemma +
   informal rank-1 channel-bandwidth barrier. Class odds ~0.005.
Best corpse (bronze declined): Half-Period Reflection Sieve -- died TWO
independent ways (reduction to Pollard p-1 w/ signed ledger + measured
r-independent sampler wall P[tag]<=C/r => Theta(r) samples). Its barrier
closes the entire "resample your way to Shor" family.

## THE SIX WALLS (all 17 deaths explained)
W1 outsourcing-oracle (key step costs the original problem);
W2 MENU-DIMENSION TRAP (fixed low-dim parameter menus = Pollard/Williams/rho
   worse-constants -- GENERALIZES the conic trap beyond curves);
W3 GNFS-wearing-a-measurement-hat (sieving variants repackaging NFS layers);
W4 multiplier-circuit message passing (certificates planted at inaccessible
   fixed points);
W5 CHANNEL-CAPACITY SATURATION (cheap global census carries O(polylog N)
   bits but exact certification needs finer resolution than channel noise;
   completion repays everything) -- appeared THREE times independently;
W6 slice-vacuity (positive claims survive only on zero-density classes where
   existing poly algorithms already win).

## THREE NEW REUSABLE SCREENING RULES (adopted)
(a) VACUITY LEMMA template (split-congruence holes bound reachable factor
    families; density <= poly(n)^c * gamma^2W / 2^n -> 0);
(b) RANK-1 BANDWIDTH BARRIER (rank-1 channels carry O(log exp l) bits total);
(c) R-INDEPENDENT SAMPLER WALL (any tag distribution independent of order r:
    P[tag] <= C/r => Theta(r) samples => exp(ln p), strictly worse than rho).

## NEXT ROUND NOMINATIONS (from wreckage)
1. Formalize W5 as a QUANTITATIVE THEOREM TEMPLATE (highest value): "any
   deterministic channel computable in time T resolves targets to f(T) bits;
   census-completion pipelines pay max(T, resolution-induced search)" --
   merge rank-1 barrier + CMP-X information wall into one magnitude-
   conditioned screening theorem.
2. Close classical-HSP escape hatches explicitly: adaptive interactive
   schedules (expect Shoup-type Omega(sqrt(p)) black-box death) +
   non-generic representation powers (enumerate short list, show each falls
   into W2 or the 8-barrier discipline). Output: closure theorem, not algorithm.
Full synthesis (zh) verbatim in workflow journal wf_37685ae0-7fa; deep-dive
derivations in task output wara6im9d.
