# exp609 CF-CASCADE-EVASION (round-75) -- findings
Fleet Bet #3: can ANY non-SQUFOF observable of sqrt(N)'s continued-fraction
cascade carry factoring-relevant information? Pre-registered; positive-control
gated; toy scale (bits 40/48).

VERDICT: **INVALID_CONTROLS (PC2), WITH PRIMARY-NULL SUPPORT AND A
FULLY-ABSORBED SECONDARY** -- recorded honestly in three layers:

LAYER 1 -- PRIMARY (geodesic-sensor face): ZERO replicated non-SQUFOF sensor
events across both populations x 8 depths x 8 features (one isolated marginal:
sum_nu2Q @K=16 in b40, 0.011 bits, non-replicated => dead by rule). The
CF-prefix layer carries NO detectable information about the Fermat oracle
variable d<=B before the SQUFOF zone. Direction supports the barrier-map row
sealing the geodesic-sensor face; formal status gated by PC2 below.

LAYER 2 -- SECONDARY (the twist that justified the run): max_nu2Q carries
~0.99 bits about v2(p+q) classes {1,2,>=3} plain (MI 0.998 vs null q99 0.017,
BOTH populations) -- apparent first new adic content since round-71. THEN THE
CONDITIONING KILL: stratifying permutations on N mod 8 drops conditional
information to EXACTLY 0.0 bits (both populations). The entire association is
absorbed by trivial modular arithmetic; nu2(Q_j)-profiles add NOTHING beyond
N mod 8. Paper 198's mod-8 cap STANDS; no new adic content. (cond_mod64
numbers ~0.86b deemed DEGENERATE: 400-stratum grid on n=800 starves the
permutation null; disclosed, not interpreted.)

LAYER 3 -- PC2 ERRATUM (why INVALID_CONTROLS): the registered estimator-
validation control demanded the SQUFOF first-square-index feature carry bits
about y*=1{d<=B} -- a WRONG-TARGET association (square forms factor via
SQUFOF's own machinery, not via small Fermat distance; measured association
~0.02 bits, below q99, both populations). Estimator validation instead served
by LAYER 2's detectable signal + tight nulls. PC2' pre-stated for future
evidentiary use: control must target an association with a MECHANISM (e.g.,
first-square-index predicting SQUFOF success itself).

Ledger catches: PC2 censoring by own depth cap (v2 amendment: uncensored
pass added); pc2 wrong target (above); mi_bits signature misuse;
ctrl_at tuple/float mismatch; argmax grid-position indexing. All caught
pre-evidence by gates/crashes; deterministic populations (pinned seeds)
make every rerun bit-reproducible.

Head-to-head arm: DEFERRED/OPEN (registered). Populations: seeds 20261113/
20261114 (unused), bands +283e6/+293e6 asserted above prior ceiling.
Wall ~15 min. Sources: exp609_cf_cascade.py, exp609_result.json,
exp609_run.log.
