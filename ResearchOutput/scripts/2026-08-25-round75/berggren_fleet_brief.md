# BERGGREN CREATIVE FLEET BRIEF (2026-08-25)
Provenance: 18-agent workflow (5 lens-generators -> 15 proposals -> 12 hostile
attacks -> synthesis). All agents read the closure record (papers 192-197),
asymptotic-goal memo, assessment barrier map. Outcome: 15 generated, 12
attacked, 12 REVISE-survivors, 0 clean-kills, 0 clean-survives. Full
synthesis verbatim in workflow journal wf_b91361fa-f37; condensed here.

## BET #1 -- STRATIFIED DOOR-AUDIT (serves asymptotic-goal item 1)
Hole found: paper 252's POOLED null (z_cal=0.65) does NOT exclude cell-wise
Dickman deviations CANCELLING across strata.
- DAY ZERO (~free): stratified cancellation test on STORED artifacts
  (exp601_counts.npz, exp598c_verify.npz) -- z_cal per N-computable cell
  (log-N decile x frac(sqrt-N) x Sum-chi+/sqrt-l tercile x kappa depth);
  look for sign-alternating per-cell deviations. GUARDRAIL: assert square-N
  before trusting stored populations (seed-20260828 lineage caveat).
- MAIN RUN (pre-registered, hash-pinned): 24k fresh odd semiprimes, 3 seeds,
  u in {4,5,6} model-validation regime + one door-rung only after expected
  minority-cell counts computed from Dickman pre-unblinding; factor-conditioned
  arm demoted to diagnostic with rows d<W excluded BY DESIGN.
Both outcomes record; exp547 triage law prices factor-conditioned families
analytically (alpha>=0.85, c<=3000).

## BET #2 -- CONIC ORDER-RIGIDITY SEALING TRIAD (theory-first, ~1% compute)
Serves asymptotic-goal item 3 (barrier-4 converse census). Three lemmas:
- PELLET-LOCK (math corrected): tr(L1^a L3^b)=2-2ab; L1L3 elliptic order 4;
  mod-p image of <L1,L2,L3> = SL2(F_p) x C2 at index (p-1)/2; EVERY word
  order divides 2p(p^2-1) => ONE shared modulus caps all words => single
  smoothness draw, no ECM variance. Re-derive or retract the exp555 story.
- PHI-SLOT: chi_w self-reciprocity kills the Phi_3 slot for every integral
  Q-preserving family; SO(Q)(F_p)=PGL_2(F_p) => tree-matrix methods =
  Pollard p-1 || Williams p+1 with worse constants -- closed BY MECHANISM.
- GENUS-CAP: order-rigidity is a genus phenomenon; rational/conic constructions
  inherit PGL2 divisors of p(p^2-1); Hasse-interval (genus>=1) is the ONLY
  structural exemption. MASTER TRICHOTOMY as mandatory check on future
  curve-family proposals (Chebotarev-invisible / CM-collapsed / torsion-zero-bits).
Only empirical leg: instantiate the tree arm AS Williams (Lucas stage-1/2 on
tr(L_w^k)), matched budgets, predict hit-rate PARITY per unit work; verify
image classification at 4000-prime strata (verifying Lagrange != evidence).
Disclose prior art: GL2 element orders, Matthews-Vaserstein-Weisfeiler,
Williams 1982, lab papers 64/67.

## BET #3 -- CF-CASCADE EVASION SHOT (the family closures don't reach by mechanism)
sqrt(N)'s continued-fraction division cascade evades closures 1/2/4 BY MECHANISM
(ambient geodesic, division-rich, unlike every sealed probe class). Template
established: closures are probe-class-LOCAL; evasion argued per family.
Design: 40-64 bit pops, 3 fresh seeds, log-spaced depth grid to 2e5;
ONE primary registered claim ("no non-SQUFOF feature carries >=0.01 bits at
any K < half first-square-index under exact conditional nulls"); mandatory
positive controls (N^{1/4} square-event front must light; graded Q-smoothness
control before square band); head-to-head vs matched-budget direct E(a)=a^2-N
sampling (paper-194 honesty probe: if CF adds nothing -> CONFIRMED-REDUCTION
closes the geodesic-sensor genre in one sentence); pre-registered detection floor.
Sharp secondary endpoint: can any division-cascade observable resolve
v2(U=p+q) beyond paper 198's mod-8 cap {1,2,>=3} via nu2(Q_j) prefix profile?
Any hit = first new adic content since round-71.

## THE CONIC TRAP (killed-pattern lesson -- adopt as screening rules)
The tree's alphabet deterministically encodes rho=(p+q)/(q-p) -- the PRIZE
coordinate -- and genus-0 ambient geometry forces every readout into one of
three dead bins: (i) circular (reads the answer), (ii) deterministic function
of N-computable data (zero conditional bits), (iii) a known method with worse
constants (SO(Q)(F_p)=PGL_2 fixes the maximal-torus menu at {p-1,p+1,p}; ECM
wins ONLY via elliptic Hasse-interval variety; genus>=1 is the unique
structural exemption -- and exemption from a cap is not a source of structure).
Instances this round: (t/N)=+1 identically on ALL Fermat-scan values;
self-reciprocity killing the Phi_3 slot; parabolic generators order-p-locked;
shallow CF prefixes reducing to matched-budget Fermat windows; MI!=channel
under wrong nulls; multiplicity portfolios firing on bugs.
SCREENING RULES (adopted at proposal stage): (a) name the claimed
maximal-torus-order variety (< elliptic = dead on arrival); (b) declare the
cost model FIRST else the probe is the scan renamed; (c) argue mechanism-level
evasion PER PROBE CLASS against the four seals; (d) deterministic-function-
calibrated nulls mandatory.

## ORACLE-BOUND REFRAME (the 0.48-bit loose end)
Upgraded from "unrealized" to REALIZATION-PRICED: ORACLE EQUIVALENCE --
1{exists m in [ceil(sqrt N), ceil(sqrt N)+B]: m^2-N perfect square} EQUALS
1{d<=B}, so realizing the bound computes p+q to precision 2B == the Fermat
scan itself (bits-per-op identical to filtered Fermat). Record the half-page
equivalence note (scope any width-query lower bound: GNFS is a standing
counterexample to unscoped Omega(d)). Sharpest remaining decidable test:
OFFSET-JUMP/TRIAGE semantics vs plain Fermat (start at floor(sqrt N)+d_hat
minus guard band; pay (d_i-d_hat)+ + guard instead of d_i) -- joint with
Bet #1's population so the same draws serve both. Prior heavily null-favored,
worth exactly one clean shot.

## RIDERS (record, don't fund)
Sieve-profilometer theory salvage rides with Bet #2 (fine-(s,delta) conditioning
= canonical kill-shot control for parabola-window class; n^2-elevation identity
prices positional sensors at rho(u_n)-rho(u_generic)). Legendre-window identity =
free two-line proof twin of paper 197. Oracle-realization + ratio-resolution =
proof-first closure notes attached to next record. Covolume census = half-day
theory-only seal. Triage-ladder breakeven derivation = negative-result-grade
pricing paper; offset-jump arm absorbed above.

## GUARDRAILS (all bets)
Fresh seeds + stream-distinctness asserts; square-N verification; smoke-n
non-evidentiary; hash-pinned pre-registrations BEFORE data; errata ride in new
papers; per-leg band stride discipline (round-95 #4 lesson).
