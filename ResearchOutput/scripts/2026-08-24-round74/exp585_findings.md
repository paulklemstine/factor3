# exp585 NEIGHBOR-SMOOTHNESS-COVARIATE (round-74, paper-234 open item pt 2) — findings
VERDICT (pre-registered): **H0 SUPPORTED** — local factorization structure around N
carries nothing beyond the QR dial.
Setup: 128 balanced bitlen-96 semiprimes, seed 20260827, REGENERATED (see provenance);
covariates omega(N±d), log-LPF(N±d), d in {-8..8}\{0} (2048 gmpy2 factorizations,
20/2048 = 1% rho-censored, zero rows imputed); QR dial S@400-family
sum_{(N/p)=1,p<=400} 2/p; per-N hits: 50k j-samples, t=j-isqrt(N)~U[1,64],
v=(isqrt+t)^2-N; PRIMARY = SIQS-partial hit (400-smooth part x one prime <= 2^40),
mu=4032/N, CV 1.6% -> measurement-noise attenuation negligible.
RESULT: QR alone R^2=0.411 (corr 0.641 — dial replication confirms pipeline live);
neighbors alone R^2=0.032; joint 0.431 => **incremental dR2 = +0.0195** (gate: >=0.05
for H1 / <0.02 for H0), permutation p=0.389 (500 shuffles; null q95=0.046 > observed —
observed increment is UNDER chance inflation for 4 predictors). Best single neighbor
|r|=0.16. Secondary hits_full (fully 400-smooth): dR2=+0.0126, same story.
READING: the ~40% u~10 residual is NOT carried by arithmetic-neighborhood smoothness
(omega/LPF of N±d) — every tested N-property class (QR dials, abelian channels,
neighbor structure) now fails to explain it; residual remains genuinely unexplained.
dR2 lands AT the 0.02 boundary but p=0.39 forecloses any effect claim.
LEDGER CATCHES:
1. PROVENANCE UNVERIFIED — exp581_regen_positions.npz contains only hit/ctl POSITION
   arrays + jlo/jhi (=3*isqrt(N), relation jlo=isqrt+1 verified), NO N array. Population
   regenerated by documented in-file recipe; isqrt fingerprint vs true lineage 0/128;
   15-variant recipe grid all 0-match (exp577_result.json unreadable this session).
   Hash 30009f0cfa7f85118d217bcacadb9d656fe5a1ae33b474b421492e828066bfef. Results are
   conditional on balanced-96-bit exchangeability (lab precedent: these laws are
   population-robust), NOT the identical-N lineage.
2. SMOKE-STAGE ENDPOINT AMENDMENT (before any lineage analysis): hits_full measured 0
   events in 160k+ synthetic draws even at t<=16 — fully-B-smooth v_j is not a measurable
   per-N endpoint at bitlen 96 (itself a finding: QS relations at this scale are ALL
   partials). Primary swapped to partial-hit count; gates unchanged.
3. Window operationalization ours (t<=64, int64-exact); canonical [isqrt+1,3*isqrt]
   range (exp578) not reproducible without exp569 source (unreadable).
Smoke PASS (0.6 s, n=12). Full wall 47.3 s (covariates 42.7 s dominant).
Files: exp585_neighbor_covariate.py (pre-reg header + amendment), exp585_smoke.log,
exp585_smoke_meta.json, exp585_full.log, exp585_result.json, this file.
